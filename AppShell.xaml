<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CalculatorWithHistory.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:CalculatorWithHistory"
    xmlns:view="clr-namespace:CalculatorWithHistory.View"
   
    >

   <!-- <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />  -->

    <TabBar>
        <Tab Title="Calculator Page">
            <ShellContent ContentTemplate="{DataTemplate  local:MainPage}" />
        </Tab>
        <Tab Title="History">
            <ShellContent ContentTemplate= "{DataTemplate view:HistoryPage}" />
        </Tab>
    </TabBar> 

<!--
    <FlyoutItem Title = "Calculator Page">
        <ShellContent
            ContentTemplate = "{DataTemplate local:MainPage}" />
    </FlyoutItem>
    <FlyoutItem Title = "History Page">
        <ShellContent
            ContentTemplate = "{DataTemplate view:HistoryPage}" />
    </FlyoutItem> -->
   

</Shell>
